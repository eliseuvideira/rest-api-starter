components:
  schemas:
    Error:
      properties:
        error:
          properties:
            message:
              type: string
          required:
            - message
      required:
        - error
